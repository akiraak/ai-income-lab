# vibeboard の「システム説明」タブ（先頭のタブ）の言葉の正本（docs/specs/dashboard.md §18）。
#
# このシステムの説明。⚠ **全体は簡単に・モデルを作る ／ 過去のデータで確かめる ／ 実際の売買で使う は手厚く**（利用者の裁定 2026-09-20）。
# ⚠ **説明を Python や HTML に書かない**（glossary.toml・models.toml と同じ考え方）。文を直すならこのファイルだけ。
# ⚠ **両方を並べる**: 本文（`lead`・`text`・`points`・`after`・`note`・`table`・図の箱の文字）は**やさしい言葉**で書く
#    （使わない語は tests/test_traders_tab.py の FORBIDDEN ＝ dashboard.md §16-2 の表。テストが見る）。
#    用語・正式な名前・コードの場所・細かい数字と出典は **`detail`・`detail_points`・`detail_table`（画面では「詳しく（用語あり）」の囲み。⚠ 畳まない・いつも開いている）にだけ**書く。
# ⚠ **本文に実売買の設定の数字（予算・買う線・見る株の本数）と `$` を書かない**（設定が変わると嘘になる。トレーダーのタブが設定から写す）。
# ⚠ **売買結果（損益・注文・約定・持ち高）は書かない**。⚠ 台帳の合計は画面が `ledger.md` から読む（`ledger = true` の段。ここに書き写さない）。
# ⚠ **型ごとのしくみ（`models = true` の段）の文は models.toml から写す**（ここに書かない）。
# ⚠ **確かめた事実だけを書く**（出どころは規約 rules.md・記録 docs/specs/experiments/・コード）。仕組みを変えたら該当の段を読み直す。
# ⚠ 図: 1 図 1 主張（`claim` は図の直前に出る）・箱は 12 個以内。`kind = "flow"`（箱と矢印）／ `"folds"`（期間を分けて試す図）。
#
# 形: [[page]]（id・label・sub・title・lead）→ [[page.section]]（title・text・points・after・note・models・ledger・links・
#     detail・detail_points・detail_links）→ [page.section.figure] ／ [page.section.table] ／ [page.section.detail_table]。
#     ⚠ TOML の決まりで、段の平の鍵（title や detail）は [page.section.figure] などの小さな表より**上**に書く。
#     links の形 ＝ { label = "…", doc = "docs/…" } か { label = "…", tab = "models|traders|system|glossary|experiments|data", item = "…" }。

[common]
detail_label = "詳しく（用語あり）"
other_pages = "ほかのページ"
no_live_models = "いま実際の売買の設定に出てくるモデルはありません。"
ledger_rows = "試した形（行）"
ledger_adopt = "採る"
ledger_hold = "保留"
ledger_drop = "落とす"
ledger_note = "過去のデータで試した結果の台帳から写した合計（台帳を作った日 {date}）。台帳を作り直すと、この数字も変わる。"

# ================================================================ 全体（簡単に）

[[page]]
id = "overview"
label = "全体のしくみ"
sub = "まずここから"
title = "このシステムのしくみ"
lead = "株の値動きから「出力スコア」（明日上がりそうかを 0〜100 で表した数）を計算するモデルを作り、過去のデータで確かめ、少ない金額で実際に売り買いして、注文が計画どおりに出るかを記録するシステム。"

[[page.section]]
title = "何をするシステムか"
text = [
  "AI を使ってお金を稼ぐ方法を、机の上で確かめるプロジェクトの一部。ここでは「株の値動きを予測して売り買いする」という方法を調べている。",
  "やっていることは 3 つ。モデルを作る → 過去のデータで確かめる → 実際の売買で使う。",
]
note = "いまのところ、過去のデータで「何もせずただ持ち続ける」を確かに上回ったモデルは 1 つも無い。実際の売買は、もうかるかを確かめるためではなく、注文が計画どおりに出て、計画どおりの値段で買えるかを確かめるために、少ない金額で行う。"

[[page.section]]
title = "全体の流れ"
after = ["左から右へ 1 本の流れになっている。上の段がモデルを作るところ、下の段が確かめて、実際の売買で使うところ。"]
links = [
  { label = "モデルを作る（手厚く）", tab = "system", item = "build" },
  { label = "過去のデータで確かめる（手厚く）", tab = "system", item = "verify" },
  { label = "実際の売買で使う（手厚く）", tab = "system", item = "live" },
]
detail = [
  "机上の検証は experiments/feature-discovery/（cli.fetch → cli.adjust → cli.build → cli.run → cli.report）。実売買は experiments/live-trading/（run-live.sh → cli.predict → run_day.py）。管理画面は dashboard/（3012）。",
  "2026-08-27 の方針で、資金を動かす実行は原則しない。例外は 2 つだけ ＝ tastytrade の API の挙動の検証（2026-09-05）と、トレーダーに予算を割り振る実売買（2026-09-17）。どちらも本番の鍵を入れて起動するのは利用者だけ（CLAUDE.md）。",
]

[page.section.figure]
claim = "モデルは作る・確かめる・使うの 3 段を通り、どの段も同じ「出力スコア」を受け渡す。"
per_row = 4
steps = [
  { t = "データを集める", s = "毎日の株の値段と、為替・金利など" },
  { t = "見ている数字を作る", s = "値動きを表す数字に直す" },
  { t = "モデルが学ぶ", s = "過去の「数字 → 次の日」の関係" },
  { t = "出力スコアを計算する", s = "明日上がりそうかを 0〜100 で", kind = "out" },
  { t = "過去のデータで確かめる", s = "ただ持ち続けるより良いか" },
  { t = "トレーダーに持たせる", s = "モデル ＋ 買う線 ＋ 予算" },
  { t = "その日の終わりに注文", s = "出力スコアが線を上回れば買う" },
  { t = "記録して見くらべる", s = "計画どおりに買えたか", kind = "note" },
]

# ================================================================ モデルを作る（手厚く）

[[page]]
id = "build"
label = "モデルを作る"
sub = "データから出力スコアが出るまで"
title = "モデルを作る — データから「出力スコア」が出るまで"
lead = "モデルは、過去の「その日の数字」と「次の日に上がったか下がったか」の組をたくさん見て、関係を学ぶ。学んだあとは、今日の数字を入れると「出力スコア」（明日上がりそうかを 0〜100 で表した数。高いほど上がりそう）を返す。"

[[page.section]]
title = "作る流れ"
after = ["どのモデルも、この 5 つの段を同じ順に通る。ちがうのは 3 つ目の「見ている数字」と 4 つ目の「学び方」だけ。だから、モデルを替えたときの差を「その段の差」として読める。"]
detail = [
  "入口は 1 実験 1 ファイルの config（experiments/feature-discovery/config/experiment/<実験名>.toml）。手法は名前で登録表（ail/registry.py）から引くので、実験を足すときはコードを書かず TOML を足すだけ。",
  "段とコマンドの対応: 集める ＝ cli.fetch（data/raw/）／ 目盛りを直す ＝ cli.adjust（data/adjusted/）／ 数字を作る ＝ cli.build（data/features/<実験>/）／ 学ぶ・出力スコアに直す・確かめる ＝ cli.run（runs/<時刻>_<実験名>/）。",
]
detail_links = [{ label = "rules.md（規約の正本）", doc = "docs/specs/experiments/feature-discovery/rules.md" }]

[page.section.figure]
claim = "どのモデルも同じ 5 段を通り、替えるのは「見ている数字」と「学び方」だけ。"
per_row = 5
steps = [
  { t = "① 集める", s = "毎日の株の値段・外の数字" },
  { t = "② 目盛りを直す", s = "株の分割でずれた値段をそろえる" },
  { t = "③ 見ている数字を作る", s = "値動きを数十〜数百の数字に" },
  { t = "④ 学ぶ", s = "数字と次の日の関係" },
  { t = "⑤ 出力スコアに直す", s = "0〜100 にそろえる", kind = "out" },
]

[[page.section]]
title = "① データを集める ② 目盛りを直す"
points = [
  "株の値段は、1 日 1 本の記録（始まり・高値・安値・終わりの値段と、売買された量）。証券会社の口座から読み取るだけで、注文の仕組みには触れない。",
  "株の外の数字も集める。為替・アメリカの金利・天気・地震など。どれも発表された日づけつきで持つ。",
  "株が分割されると、値段が急に半分や 4 分の 1 になったように見える。本当の暴落と取りちがえないよう、分割のぶんを直してから使う。",
  "集めたままのデータは書き換えない。直したものは別の置き場に作る。",
]
detail = [
  "足は tastytrade（読み取りだけ）。外部系列は config/dataset/exog_daily.toml など（為替・イールド・NOAA の気象・USGS の地震）。置き場は data/raw/ → data/adjusted/ → data/features/ の 3 層で、raw は書き換えない（rules.md 1 章）。",
  "調整は株式分割だけ（配当は未調整 ＝ 価格リターン）。継ぎ目は cli.adjust --period d --report で見てから書く。本物の暴落を消さないための判別は rules.md 2-4。",
  "⚠ 実売買の日足は別の置き場 data-live/（live_update.sh）。配信側が過去の足をさかのぼって変えるので（63 銘柄中 41 銘柄に 2% 超の食い違い【実測 2026-09-19】）、研究用の data/ は凍らせ、data-live/ は研究用の写しを種にして後ろだけ継ぎ足す。",
]
detail_links = [{ label = "データのタブ", tab = "data", item = "overview" }, { label = "live-trading.md §0-9 (b)", doc = "docs/specs/experiments/live-trading.md" }]

[[page.section]]
title = "③ 見ている数字を作る"
text = ["値段そのものは株ごとに大きさがちがうので、そのままでは学べない。「どれだけ上がったか」「どれだけ荒いか」のような、株をまたいで同じ意味になる数字に直す。数字にはまとまりがあり、モデルごとに使うまとまりを選ぶ。"]
note = "決まり: 今日の数字は、今日までのデータだけから作る。明日以降のデータが 1 つでも混ざると、過去のデータでの成績だけが良くなる（先を見てしまう）。これはテストで毎回確かめている。"
detail = [
  "層と接頭辞（rules.md 6 章）: own_（35 列）／ cs_（断面）／ rel_（市場・セクターに対する相対。ETF は持てないので会社株だけ）／ ex_（価格の外。1 日以上ずらす ex_lag_days = 1・古すぎる値は使わない ex_max_stale_days = 7）／ seq（過去 60 営業日の対数リターンの窓）／ trend（窓 20・60・200 の 6 列）。コードは ail/features/。",
  "先読み禁止は rules.md 7 章。足 i の特徴量は足 i までしか見ない（rolling は後ろ向きだけ・shift(-k) はラベルを作る 1 か所だけ）。",
]

[page.section.table]
head = ["まとまり", "中身", "使うモデルの例"]
rows = [
  ["その株の値動き", "上がり下がり・荒さ・いまの位置・売買された量・値動きの形・曜日", "値動きの数字を足し合わせる型"],
  ["ほかの株とのくらべ", "今日の上がり方は全体の何番目か、全体の何割が上がったか", "世の中の数字も入れて、枝分かれで決める型"],
  ["市場とのくらべ", "市場全体や業種が動いたぶんを引いた、その株だけの動き", "同上"],
  ["株の外の数字", "為替・金利・天気・地震（1 日以上前に発表されたものだけ）", "同上"],
  ["過去 60 日の線", "今日の終わりの値段を 0 として、過去 60 日の値段がそこからどれだけ上か下かを並べた 1 本の線（毎日の上がり下がりをつみ上げたもの）", "過去 60 日の線の形を読む型"],
  ["長めの調子", "20 日・60 日・200 日で見た、平均からの離れ・傾きなど", "上がり調子かどうかを見る門"],
]

[[page.section]]
title = "④ 学ぶ"
text = [
  "答えは「その日の終わりの値段から、次の日の終わりの値段までに、どれだけ上がり下がりしたか」。モデルは、数字の組からこの答えを言い当てる練習をする。",
  "学び方がモデルの型のちがいになる。重みをかけて足す型、「もし〜なら」の枝分かれを重ねる型、数字を何段もまぜ合わせる型、線の形を読む型などがある。",
]
points = [
  "数字の大きさをそろえる作業（平均を 0 に、ばらつきを 1 に）も、学ぶ期間のデータだけで決める。答え合わせの期間のデータは使わない。",
  "学び方の細かい設定（枝の数・学ぶ速さなど）は最初に決めて動かさない。結果を見ながら設定をいじると、試した数だけたまたまを拾いやすくなるため。",
]
links = [{ label = "モデルの型の一覧（予測モデルのタブ）", tab = "models", item = "all" }]
detail = [
  "ラベル y ＝ 足 t の終値 → 足 t+1 の終値の対数リターン（horizon = 1。rules.md 8 章）。モデルは回帰で y を当てにいく（分類にしない ＝ rules.md 13-2）。",
  "モデルは ail/models/（Ridge α=1.0 ／ LightGBM num_leaves 31・lr 0.05・n_estimators 500・早期打ち切り ／ MLP 64→32→1 ／ PatchTST）、検知器は ail/detectors/、選別は ail/selectors/。選別・標準化・PCA は訓練分割の内側だけで fit する（rules.md 9 章 規約 4）。ハイパーパラメータは事前固定（11 章 規約 4）。",
]

[[page.section]]
title = "⑤ 出力スコアに直す"
text = [
  "モデルの生の答えは「明日は少し上がりそう」くらいの小さな数で、そのままでは買う・買わないの線を引きにくい。そこで 0〜100 の出力スコアに直す。",
  "直し方: 学ぶ期間の終わりのほうを少し取り分けておき、「生の答えがこのくらいだった日に、実際に上がったのは何割か」を測って対応を作る。まん中の 50 は「どちらとも言えない」。",
]
note = "過去のデータで試すと、どのモデルも出力スコアがまん中の近くのせまい幅に集まった。つまり「強く上がりそう」と言える日がほとんど無い。これが、買う線を上げるとほとんど買わなくなる理由。"
detail = [
  "Platt 較正（ail/models/calibrate.py）: 買い% ＝ 100·σ(a·pred + b)。a・b は訓練分割の尻の holdout で fit する（rules.md 13-2）。出口% は 100 − 買い%（入口と出口を別に持つ検知器だけ別の窓で fit ＝ 16 章）。",
  "門の診断【実測】: 時系列分類器 3 本の holdout AUC は 0.501〜0.508・買い% の幅は 0.5〜2.1 ポイント（tsc-threshold.md §3）。(A) 形式の Platt 係数は a ≈ 0 だった（threshold-trading.md）。⚠ 2026-09-12 より前は較正の数値解が止まっていた（較正「旧」。台帳では列で区別する）。",
]
detail_links = [{ label = "tsc-threshold.md", doc = "docs/specs/experiments/tsc-threshold.md" }, { label = "threshold-trading.md", doc = "docs/specs/experiments/threshold-trading.md" }]

[[page.section]]
title = "型ごとのしくみ（いま実際の売買の設定に出てくるモデル）"
text = ["③〜⑤ の中身を、型ごとに図にしたもの。どの型が出るかは実際の売買の設定から引いている。"]
models = true
links = [{ label = "机上で試しただけの型もふくめた一覧", tab = "models", item = "all" }]

[[page.section]]
title = "新しいモデルを足すとき"
points = [
  "学び方や数字の作り方を 1 つ書いて、名前を付けて登録する。あとは設定のファイルを 1 枚足すだけで、作る → 確かめるの流れに乗る。",
  "回すと決めるのは、結果を見る前。回したものは、うまくいかなくても全部「試した数」に数える。",
  "計算が変わる変更をしたら、必ず名前も変える。同じ名前のまま中身を変えると、前の結果と混ざってしまう。",
]
links = [{ label = "名前と鍵", tab = "system", item = "names" }]
detail = [
  "登録は @register(種類, 名前)（ail/registry.py）。新しいファイルを作ったら ail/bootstrap.py に 1 行足す。可能性が低くても机上の検証は積極的に回す（rules.md 14-10）。緩めないもの ＝ 回すと決めるのは結果を見る前 ／ 回したものは全部 n_trials に数える ／ leak 対照 ／ 1 実行 1 ディレクトリ。",
]

# ================================================================ 過去のデータで確かめる（手厚く）

[[page]]
id = "verify"
label = "過去のデータで確かめる"
sub = "ものさし・判定・これまでの経緯"
title = "過去のデータで確かめる — ものさしと判定"
lead = "作ったモデルが役に立つかを、過去のデータの上で売り買いのまねごとをして確かめる。ものさしは「何もせずただ持ち続けるより良いか」。"

[[page.section]]
title = "期間を分けて、先へ進みながら試す"
text = ["過去のデータ全部で学んで、同じデータで答え合わせをすれば、成績は必ず良く見える。そこで期間を分け、「ここまでで学ぶ → その先で答え合わせ」を、時間の向きに進みながら 5 回くり返す。"]
points = [
  "答え合わせの期間は、学ぶときに 1 度も見せない。",
  "学ぶ期間と答え合わせの期間のあいだに、すき間を空ける（答えが次の日にまたがるので、境目の日を捨てる）。",
  "1 回の答え合わせは 1 年半ほど。値下がりの大きかった時期も、上がり続けた時期もふくまれる。",
]
detail = [
  "ウォークフォワード（増えていく窓）。split = walk_forward_dates・folds = 5・purge = true（config の [validation]）。切れ目は全銘柄共通の日付集合を 6 等分して決める（ail/validation/splits.py の date_edges。最初の 1 塊は訓練だけ）。own_2018 の表では fold 1 ＝ 2019-07-09〜2020-12-08（360 日）・fold 2 ＝ 2020-12-09〜2022-05-16（361 日）…【実測。threshold-trading.md】。",
  "パージ ＝ ラベルが検証期間に食い込む訓練行を落とす。断面を使うときはエンバーゴ（境目をさらに空ける）。時系列を無作為に切らない（rules.md 9 章）。",
]

[page.section.figure]
kind = "folds"
claim = "学ぶ期間はだんだん長くなり、答え合わせはいつも「学んだ先」の期間で行う。"
n = 5
row = "試し"
learn = "学ぶ期間"
gap = "すき間（捨てる）"
test = "答え合わせの期間"
unused = "まだ使わない"
axis_from = "古い"
axis_to = "新しい"

[[page.section]]
title = "売り買いのまねごと"
text = ["答え合わせの期間では、毎日の出力スコアを見て、決まりどおりに売り買いしたことにする。"]
points = [
  "株ごとに「持っている」か「持っていない」かの 2 つだけ。出力スコアが買う線を上回ったら買い、売る線を下回ったら売る。買い増しも、値下がりでもうける売り方もしない。",
  "売り買いした日にだけ、手数料に当たる小さな費用を引く。費用を引いたあとの成績で判断する。",
  "買う線は 3 通り（まん中・少し上・もっと上）を最初に決めてあり、結果を見て動かさない。",
  "成績は、見ている株を同じ重さでまとめた 1 本の成績で測る。株 1 本ごとの勝ち負けでは判断しない（勝った株だけを見てしまうため）。",
]
detail = [
  "rules.md 13 章。0 か 1 の状態機械・買い専用。足 t の特徴量 → 足 t の終値で執行 → y_t を得る。コストは cost_bp = 5.0（往復【推測】）の半分 2.5bp を建てた日と手仕舞った日にだけ引く。fold の末尾で強制清算。θ ∈ {50, 55, 60} を事前固定（13-3）。形式 (A) 銘柄共通 ／ (B) 銘柄別（13-6）。",
  "採否はポートフォリオ日次純利（等加重）の対 B&H 上乗せで測る。銘柄別 bp は成果物で、採否には使わない（13-7）。",
]

[[page.section]]
title = "ものさしと判定"
text = ["くらべる相手は「最初に全部買って、何もせずただ持ち続ける」。株は長い目で見ると上がる日のほうが多いので、ただ持つだけでも成績は良くなる。モデルを使う意味があるのは、これを上回ったときだけ。"]
note = "「保留」は「採るの一歩手前」ではない。平均を押し上げたのが 1 つの期間だけ、という形がほとんどで、たまたまと区別がつかない。"
detail = [
  "判定は ail/catalog.py の judge が summary.csv と checks.json から機械的に決める（rules.md 13-7）: 上乗せ > 0 かつ fold の上乗せ符号が全部正 ＝ 採る ／ 上乗せ > 0 だが符号が割れる ＝ 保留 ／ 上乗せ ≤ 0 ＝ 落とす（基準線以下 ＝ 何も学んでいない）。DSR を通すまでは「採る」でも根拠「中」が上限（11 章 規約 3）。",
  "基準線は同じシミュレータで回す（13-5）: B&H・常に上（ドリフト）・直前リターンの符号・全部使う・乱択、レジーム系には乱択ゲート（15-6）、上位 K にはボラ上位（17-7）。",
]

[page.section.table]
head = ["印", "意味"]
rows = [
  ["採る", "5 回の答え合わせの全部で、ただ持ち続けるを上回った"],
  ["保留", "平均では上回ったが、回によって勝ち負けが割れた"],
  ["落とす", "平均で上回らなかった"],
]

[[page.section]]
title = "配線がまちがっていないかの検査"
text = ["成績が悪いとき、「モデルが当てられなかった」のか「仕組みがこわれていて当たるはずがなかった」のかを分けたい。そこで、わざと明日の答えを数字に混ぜた「ずるい版」も毎回いっしょに回す。"]
points = [
  "ずるい版の成績が大きく跳ね上がれば、データ → 数字 → モデル → 売り買いの配線はつながっている。",
  "跳ね上がらなければ、どこかがこわれている。そのときの本番の結果は読まない。",
]
detail = ["leak 対照（rules.md 7 章・13-10）。--leak で回した実行はディレクトリ名に _leak が付く（config には無い）。例: PatchTST の leak 対照は上乗せ ＋16,363〜＋17,160bp・t 7.89〜11.41 に跳ねた ＝ 配線は健全で、学べなかったのはモデルの側（patchtst-threshold.md §5）【実測】。"]

[[page.section]]
title = "試した数を数える"
text = ["サイコロを何百回もふれば、たまたま良い目が続くことがある。モデルも同じで、たくさん試すほど、たまたま良く見える形が出てくる。だから試した形は全部、台帳に数えて残す。良い数字が出たら、「これだけ試した中でのいちばん」として割り引いて読む。"]
ledger = true
detail = [
  "台帳 ＝ docs/specs/experiments/feature-discovery/ledger.md（生成物。cli.report --catalog。手で書き換えない）。多重検定に使う n_trials は台帳の行数で、デフレーテッド SR（DSR）に渡す。⚠ 試行を増やしても採否は厳しくならない（判定式に DSR は入らない ＝ rules.md 14-10）。",
  "条件付き GAN のシナリオ予測は別の物差し（CRPS）なので台帳の n_trials は動かさず、試した構成は記録の §0-3 に数える（cgan-scenario.md）。",
]
detail_links = [{ label = "ledger.md（台帳）", doc = "docs/specs/experiments/feature-discovery/ledger.md" }, { label = "検証のタブ", tab = "experiments", item = "overview" }]

[[page.section]]
title = "これまでの経緯"
text = ["2026 年 9 月に試したものを、日づけの順に並べた。どれも「採る」は無かった。"]
links = [{ label = "モデルの型ごとの解説と印（予測モデルのタブ）", tab = "models", item = "all" }]
detail = ["数字はすべて記録からの写し【実測】。上乗せ ＝ 対 B&H の純利の差（bp/fold）。"]
detail_links = [
  { label = "gpu-models.md", doc = "docs/specs/experiments/gpu-models.md" },
  { label = "threshold-trading.md", doc = "docs/specs/experiments/threshold-trading.md" },
  { label = "downtrend-detection.md", doc = "docs/specs/experiments/downtrend-detection.md" },
  { label = "entry-timing.md", doc = "docs/specs/experiments/entry-timing.md" },
  { label = "mlp-threshold-trading.md", doc = "docs/specs/experiments/mlp-threshold-trading.md" },
  { label = "gan-threshold-trading.md", doc = "docs/specs/experiments/gan-threshold-trading.md" },
  { label = "selectors-seven-threshold.md", doc = "docs/specs/experiments/selectors-seven-threshold.md" },
  { label = "tsc-threshold.md", doc = "docs/specs/experiments/tsc-threshold.md" },
  { label = "patchtst-threshold.md", doc = "docs/specs/experiments/patchtst-threshold.md" },
  { label = "evolutionary-search.md", doc = "docs/specs/experiments/evolutionary-search.md" },
  { label = "topk-holdings.md", doc = "docs/specs/experiments/topk-holdings.md" },
  { label = "cgan-scenario.md", doc = "docs/specs/experiments/cgan-scenario.md" },
]

[page.section.table]
head = ["日づけ", "試したもの", "結果"]
rows = [
  ["9/9", "計算の速い部品を使った型（枝分かれ・まぜ合わせ・にせデータの足し込み）を、毎日売り買いする形で", "採るは無し。にせデータの足し込みは落とす"],
  ["9/10", "ものさしを「買う線つきの売り買い」に替えて、足し合わせと枝分かれの型を試し直した", "24 通りのうち 23 通りは落とす・1 通りは保留"],
  ["9/12", "上がり調子かどうかを見る門（20 日・60 日・200 日）", "21 通りのうち 18 通りは落とす・3 通りは保留（計算を直したあとは 17 通りと 4 通り）。昔からある決まりの門は下げを避けられたが、戻りを取りそこねた"],
  ["9/12", "買うときと売るときで、見る長さを変える門", "36 通りのうち 34 通りは落とす・2 通りは保留。この方向は続けない"],
  ["9/13", "数字を何段もまぜ合わせる型", "12 通りとも落とす"],
  ["9/13〜14", "にせの過去データを足して学ばせるやり方（6 種）", "36 通りとも落とす（見ている数字を替えた形でも同じ）"],
  ["9/15", "見ている数字をしぼる選び方 7 通り", "42 通りとも落とす"],
  ["9/15", "過去 60 日の線の形を読む型（3 種）", "9 通りのうち 8 通りは落とす・1 通りは保留"],
  ["9/15", "60 日の線を切れはしに分けて読む型", "3 通りとも落とす。何も学ばなかった"],
  ["9/16", "式を競わせて進化させる型", "9 通りとも落とす。学ぶ期間でだけ当たる式を見つけてきた"],
  ["9/19", "出力スコアの高い順に、持つ株の数をしぼる決まり", "採るは無し。モデルを使わず値動きの荒い順に選ぶほうが良かった ＝ 順位に中身は無かった"],
  ["9/19", "これから 5 日の筋書きをたくさん作る型", "決めておいた 5 つの条件を 1 つも満たさず、落とす"],
]

[page.section.detail_table]
head = ["記録", "内訳と数字"]
rows = [
  ["gpu-models.md（2026-09-09）", "毎日往復の物差し。勾配ブースティング・深層 ＝ 保留（1 期間依存）／ 断面 × 非線形 ＝ 落とす〜保留 ／ GAN 増強 ＝ 落とす ／ 系列モデル ＝ 見送り"],
  ["threshold-trading.md（09-10）", "追試 24 試行: 落とす 23 ／ 保留 1（own × Ridge × (A) × θ=50。上乗せ ＋12.8bp・t 0.13・符号 1/5。⚠ 較正「旧」の行。いまのコードでは落とす）"],
  ["downtrend-detection.md（09-12）", "21 試行: 落とす 18 ／ 保留 3 ／ 採る 0。保留はどれも t < 2・符号が割れる。較正 std の回し直し（20-40-14）は 落とす 17 ／ 保留 4（D1 θ50 が保留へ）"],
  ["entry-timing.md §9（09-12）", "36 試行: 保留 2 ／ 落とす 34。最良 ＋29.56bp/fold・符号 1/5・t 0.87・DSR 0.267。上位 3 行は対照側（逆向き）"],
  ["mlp-threshold-trading.md（09-13）", "own 6 行: 上乗せ −68〜−1,017bp ／ ownex 6 行: −312〜−703bp"],
  ["gan-threshold-trading.md（09-13〜14）", "own 36 行: 上乗せ −36.70〜−1,638.70bp。ownex（gan-threshold-ownex.md）も 36 行とも落とす"],
  ["selectors-seven-threshold.md（09-15）", "選別 7 本 × Ridge / LightGBM: 42 行とも落とす"],
  ["tsc-threshold.md（09-15）", "MiniRocket・Hydra・QUANT × 3 θ: 落とす 8 ／ 保留 1（QUANT θ=50 ＋61.92bp・t 0.44。5 fold 中 4 fold が「全部持つ ／ 全部休む」）"],
  ["patchtst-threshold.md（09-15）", "上乗せ −51.11 ／ −989.89 ／ −1,568.40bp。学習 10 回とも検証 MSE が「0 と予測する」を超えられない"],
  ["evolutionary-search.md（09-16）", "F4-1 記号回帰 × Ridge・LightGBM・MLP × 3 θ ＝ 9 行とも落とす（Ridge: −219.08〜−698.59bp）。先頭の式に own_dow（曜日。5 fold 中 4 fold。fold 2 には無い）"],
  ["topk-holdings.md（09-19）", "台帳の 45 行: 採る 0 ／ 保留 37 ／ 落とす 8。基準線「ボラ上位」が T1・T3 の端数 10 対のうち 9 対でモデルの順位を上回った"],
  ["cgan-scenario.md（09-18〜19）", "CRPS（低いほどよい）: cGAN 0.01210 ／ 履歴ベース 0.01195 ／ 軽量モデル 0.01146。事前登録の 5 条件とも満たさず"],
]

[[page.section]]
title = "この確かめ方の限界"
points = [
  "試しに使った株は、いま大きい会社から選んである。過去に消えた会社が入っていないので、成績は実力より良く見える。",
  "その日の終わりの値段で必ず買えて売れることにしている。実際にそのとおりの値段で買えるかは、実際の売買で確かめる。",
  "小さな効き目は、この長さのデータでは「ある」とも「無い」とも言い切れない。",
]
detail = ["rules.md 12 章（既知の限界）: 生存バイアス・終値執行・コスト固定・1 会場のデータ。検出限界は 14-2・validation-power.md。"]
detail_links = [{ label = "validation-power.md", doc = "docs/specs/experiments/feature-discovery/validation-power.md" }]

# ================================================================ 実際の売買で使う（手厚く）

[[page]]
id = "live"
label = "実際の売買で使う"
sub = "1 日の流れ・安全の仕掛け・出力スコアの読み方"
title = "実際の売買で使う — モデルの出力スコアが注文になるまで"
lead = "確かめたモデルを「トレーダー」に持たせ、少ない金額で実際に売り買いする。見るのは、もうかったかではなく、注文が計画どおりに出て計画どおりの値段で買えたか。"

[[page.section]]
title = "トレーダー ＝ モデル ＋ 買う線 ＋ 予算"
text = ["トレーダーは、売り買いを決める 1 つの単位を人に見立てたもの。それぞれが、使うモデル（1 本以上）・買う線・予算・見る株を持つ。どれも始める前に決めて、設定のファイルに書いてある。途中でモデルや買う線を替えるのは「新しい人」として扱う。"]
links = [{ label = "トレーダーのタブ（人ごとの決まり）", tab = "traders" }]
detail = [
  "設定は experiments/live-trading/config/traders/<名前>.toml（予算 budget_usd・銘柄集合 universe・[[models]] の kind / name / method・合成規則 combine・θ threshold・sizing）。確定した 3 人は T1 ＝ trade_own_ridge_a θ=50 ／ T2 ＝ trade_ownex_lgbm_a θ=55 ／ T3 ＝ trade_ownseq_ridge_a の T3 QUANT θ=50（2026-09-19 利用者決定。選んだ基準は族の違いで成績ではない ＝ live-trading.md §0-1）。",
  "⚠ 銘柄集合と sizing は本番の dryrun2（端株が通るか）で決まるまで candidates/{notional,shares}/ に置いてある。配線は試験用の test_a（固定の合図・1 銘柄・最小額）で先に通す。",
]
detail_links = [{ label = "live-trading.md", doc = "docs/specs/experiments/live-trading.md" }]

[[page.section]]
title = "1 日の流れ"
after = [
  "動くのは 1 日 1 回、取引が終わる直前だけ。その時の値段までを見て今日の出力スコアを計算し、その場で注文する。日中は何もしない。",
  "モデルは毎日、その日までのデータで学び直す。確かめるときと同じ計算をそのまま使い、実際の売買のためだけの別の計算は足していない。",
  "出力スコアが 1 本でも出せなかった日は、注文の仕組みを起こさない（古い出力スコアや欠けた出力スコアで売り買いしない）。",
]
detail = [
  "run-live.sh の 1 本。朝 ＝ --prepare（日足 ＋ 外部系列の更新 約 2 分 → 紙上の対照 out/daily.csv。発注しない）。15:50 ET ＝ --wait で起こす → 日足の更新（足だけ 52 秒）→ cli.predict × 3（並列 36 秒）→ out/<日付>/predict.jsonl → 台帳の控え state-backup/（submit の回だけ）→ run_day.py【所要時間は実測・titan】。",
  "cli.predict は表 ＝ cli.build.assemble・買い% ＝ cli.run.fold_buy_pct の切り出しで、モデル・較正のコードは足していない。訓練 ＝ ラベルが asof より前に確定している行 ／ 検証 ＝ asof の行。asof より後の足は読んだ直後に捨てる。runs/ を作らない（試行ではない）。",
  "執行の窓は 15:45〜16:05 ET（NYSE の営業日。休場日は暦で見る。半日立会の日は発注しない）。合図は 15:50 の気配、注文は成行。未約定は発注から 600 秒で取消。Session offline と 5xx は 60 秒おき 3 回まで再送（external-identifier で重複を探してから）。",
]
detail_links = [{ label = "live-trading.md §0-9", doc = "docs/specs/experiments/live-trading.md" }]

[page.section.figure]
claim = "1 日の仕事は取引が終わる直前の数分に集まっていて、出力スコア → 注文 → 確認の順に 1 本で流れる。"
per_row = 4
steps = [
  { t = "朝: 準備", s = "前の日までの値段と外の数字を取りこむ（注文しない）" },
  { t = "終わる直前: 今日の値段", s = "その時までの値段を取りこむ" },
  { t = "学び直して今日の出力スコア", s = "モデルごとに、株 1 本ずつ", kind = "out" },
  { t = "トレーダーが決める", s = "出力スコアと買う線をくらべる・予算を見る" },
  { t = "控えを書いてから注文", s = "全部の注文を順に出す" },
  { t = "買えたかを確かめる", s = "1 本ずつ確かめて帳面に書く" },
  { t = "買えなかった注文は取り消す", s = "しばらく待っても買えないとき" },
  { t = "あとで見くらべる", s = "計画の値段・買えた値段・終わりの値段", kind = "note" },
]

[[page.section]]
title = "出力スコアから注文へ"
points = [
  "出力スコアが買う線を上回った株を買い、売る線を下回った株を売る。どの株にも同じ金額しか入れない（出力スコアが高くても多く買わない）。",
  "モデルを 2 本以上持つトレーダーは、出力スコアの合わせ方（平均・多数決・全部そろったとき）を最初に決めておく。",
  "注文は、トレーダーごとに別々に出す。ちがう人の買いをまとめたり、ある人の売りと別の人の買いを内々で打ち消したりしない。1 人ずつの実績を正しく残すため。",
  "予算を超える買いは、注文の仕組みが断る。1 日に何回売り買いしても同じ。",
  "売ったお金は、次の日まで次の買いに使わない。",
]
detail = [
  "合成規則 combine ＝ asis / mean / majority / unanimous（事前固定）。sizing ＝ notional（金額指定・端株）か shares（整数株）。空き ＝ 予算 − 建玉の取得原価 − 受渡し待ちの売却代金（T+1・保守側）。超える買いは over_budget / too_small の事象として記録して見送る（plan.size_intents）。",
  "発注は 2 段（利用者決定 D6）: ① 全部の注文を順に 控え → dry-run → 発注 ／ ② 1 本ずつ約定を確かめて台帳へ。直列（--serial）では初日の約 120 本が窓に収まらない（モック 120 本: 直列 124 秒 ／ 2 段 2.4 秒【実測】）。",
  "口座への注文はトレーダーごとに別々（合算しない・按分しない・内部移転しない ＝ 2026-09-19 利用者決定）。同日に A の売りと B の買いが重なっても両方を口座に出し、売りが先。",
]

[[page.section]]
title = "帳面と口座を合わせる"
text = ["だれがどの株をいくつ持っているかは、トレーダーごとの帳面に付ける。証券会社の口座には全員ぶんがまとめて入っているので、帳面の合計と口座が合っているかを毎回確かめる。"]
points = [
  "注文を出す前に、必ず控えを書く。途中で止まっても、次に起きたときに控えから「出したはずの注文」を調べて帳面に戻せる。",
  "口座のほうが多い株は「帳面の外」として記録するだけ。口座のほうが少ない株は、その日は売り買いしない。",
  "食いちがいを、だれかのものと推し量って割り振ることはしない。合わせるのは人。",
]
detail = ["3 段（live-trading.md §0-8）: 発注の前に控え state/<env>/journal.jsonl を書き、起動時に未完を照会してその人の台帳に戻す ／ 口座 − 台帳の合計を突き合わせる ／ 人が reconcile.py で合わせる（ネットワークなし）。台帳の保存は 1 注文ごと。状態は state/<env>/<名前>.json、記録は out/<日付>/*.jsonl（Masker 経由・git 管理外）。"]

[[page.section]]
title = "安全の仕掛け"
detail = [
  "鍵の 3 段（ttclient.Client）: dry-run ＝ TT_ALLOW_PROD_DRY_RUN=1 ／ 取消 ＝ allow_prod_cancel（管理画面の停止ボタン用）／ 発注 ＝ TT_ALLOW_PROD_ORDERS=1 ＋ --i-know-this-is-real-money。取消の鍵で発注は開かない。⚠ 本番の鍵を入れて起動するのは利用者だけ。",
  "停止 ＝ 記録ディレクトリの HALT。管理画面（3012）の停止ボタンが置き、働いている注文を全部取り消す。⚠ 管理画面に発注の経路は無い。予算の上限は規模 A $1,000（既定）。規模 B $10,000 は --max-total-budget 10000 --max-day-usd 10000 を明示したときだけ（⚠ 実際の取引では使えない可能性がある）。含み損 20% は drawdown_warning（警告だけ）。",
  "シミュレーション ＝ 機械のモード MODE（無ければ real）・run.lock・記録は sim/<名前>/ に分け全行 sim: true・トレーダー名は sim_ 始まり。sim のとき本物の run_day は鍵があっても起動を拒否する（rc=5）。回す機械は資格情報を置かない Sx360（live-trading.md §0-7）。",
]

[page.section.table]
head = ["仕掛け", "中身"]
rows = [
  ["鍵は 3 段", "注文の下書きを確かめる ／ 取り消す ／ 本当に注文する、でそれぞれ別の鍵が要る。取り消しの鍵では注文できない"],
  ["本番の鍵を入れるのは人", "AI は手順とコードまで。鍵を入れて起こすのは利用者だけ"],
  ["停止ボタン", "管理画面のボタン 1 つで、新しい注文を止め、出ている注文を全部取り消す"],
  ["予算の上限", "始める前に書いた上限を超える買いは、注文の仕組みが断る"],
  ["時間の窓", "取引が終わる前後の決まった時間の外では注文しない。休みの日は暦で見る"],
  ["大きな含み損", "警告を出すだけ。自動で投げ売りしない。止めるのは人"],
  ["シミュレーション", "仮のデータと仮の時計で、同じ仕組みを通しで動かせる。本物とは同時に動かず、記録も画面も必ず分ける"],
]

[[page.section]]
title = "出力スコアの読み方"
points = [
  "出力スコアは 0〜100。まん中の 50 は「どちらとも言えない」。まん中から少し外れているだけの日がほとんど。",
  "出力スコアが高いことは、「大きく上がる」という意味ではない。上がる見こみがまん中より少し高い、という意味。",
  "同じ日に、どの株にもほとんど同じ出力スコアをつけるモデルがある。そのモデルを使うトレーダーは、全部買うか 1 本も買わないかになりやすい。何日も買わなくても故障ではない。",
  "出力スコアの出かたの癖はモデルごとにちがう。トレーダーのページの「モデルの特性」に書いてある。",
]
links = [{ label = "予測モデルのタブ", tab = "models", item = "all" }]
detail = ["2026-09-18 の合図【実測】: T1 買い 58 ／ 63・T2 買い 0 ／ 48（全銘柄 44〜46% ＝ θ 55 に届かない）・T3 買い 62 ／ 63 ＝ 初日の注文は 120 本前後（live-trading.md §0-9 (a)）。"]

[[page.section]]
title = "何を見て判定するか"
text = ["見るのは「差」。モデルが出力スコアを計算した時の値段・実際に買えた値段・その日の終わりの値段の 3 つをくらべ、過去のデータでの試しが前提にしていた「終わりの値段で買える」が、どこまで本当かを測る。あわせて、人の手を借りずに毎日動き続けられるかを見る。"]
note = "もうかったかどうかでは、モデルを採らない。数週間の売り買いでは、実力かたまたまかを見分けられないため。"
detail = [
  "判定の閾値（20 営業日。プラン §2-4）: 執行価格の差 中央値 ≤ 5bp ／ 紙上 − 実物 ≤ 5bp/日 ／ 人手 0 回 ／ 執行できた日 95% 以上。紙上の対照は paper.py → out/daily.csv（読むだけの後処理）。",
  "管理画面（dashboard/・3012）の概要・全体の詳細・トレーダーの詳細が、執行器の記録を写して差 1〜4 と損益を出す（dashboard.md §13）。⚠ 3012 は tailnet に出さない。",
]
detail_links = [{ label = "dashboard.md §13", doc = "docs/specs/dashboard.md" }, { label = "live-trading-three-models.md（プラン）", doc = "docs/plans/live-trading-three-models.md" }]

# ================================================================ 名前と鍵（技術的な定義）

[[page]]
id = "names"
label = "名前と鍵"
sub = "技術的な定義"
title = "名前と鍵 — モデルは内部でどう呼ばれているか"
lead = "モデル 1 本を指すのは、2 つの名前の組。「どの試しの設定か」と「その中のどの手法か」。記録や設定のファイルには、この 2 つがそのままの綴りで出てくる。"

[[page.section]]
title = "モデル 1 本 ＝ 設定の名前 × 手法の名前"
text = ["1 つの試しの設定の中に、手法が何本も入っていることがある。だから設定の名前だけでは、モデル 1 本は決まらない。実際の売買の設定・今日の出力スコアのファイル・画面の解説は、どれもこの 2 つの名前の組で同じモデルを指している。綴りが 1 文字でもちがうと別のものとして扱われ、注文の仕組みは止まる。"]
detail = [
  "実売買の設定の [[models]]: kind = \"experiment\" ／ name ＝ 実験名（例 trade_ownseq_ridge_a）／ method ＝ 手法名（例 T3 QUANT（60日窓）＝ その実験の結果表の「手法」の列 ＝ 選別か検知器の登録名。学習器の名前ではない）。",
  "predict.jsonl の行は model（実験名）・method・symbol・buy・exit。signals.py は (実験名, 銘柄) で引き、行の method が設定と違えば SignalError で止まる。シミュレーションの作り置きのファイル名は 実験名@<手法名の sha1 の頭 8 桁>。",
  "画面: traderview.find_model は name と method の両方が一致する models.toml の [[model]] だけを当てる。id（例 seq-quant）は #models/<id> の URL 用で、内部の鍵ではない。",
]

[page.section.figure]
claim = "同じ 2 つの名前の組が、設定・今日の出力スコア・画面をつないでいる。"
per_row = 4
steps = [
  { t = "試しの設定", s = "設定の名前 ＋ 中の手法の名前" },
  { t = "実際の売買の設定", s = "トレーダーが使うモデルとして書く" },
  { t = "今日の出力スコアのファイル", s = "1 行ごとに 2 つの名前つき" },
  { t = "画面の解説", s = "同じ組で当てて、やさしい呼び方を出す", kind = "out" },
]

[[page.section]]
title = "部品の名前と、台帳の鍵"
text = ["学び方・線の読み方・数字の選び方などの部品は、名前を付けて登録表に入れてある。試しの設定は、この名前で部品を指す。"]
points = [
  "部品の名前は、試した結果の台帳で「同じ形か、ちがう形か」を見分ける鍵になっている。だから、計算が変わる変更をしたら必ず名前を変える。",
  "試しの設定の名前（ファイル名）は鍵ではない。台帳は、設定の中身（モデル・見ている数字・期間・線の引き方など）から鍵を作る。",
  "すでにある名前は変えない。これから足す名前だけ、決まった形にそろえる。",
]
detail = [
  "登録は ail/registry.py の register(kind, name)。種類は source / feature / selector / transform / model / detector / split / metric / stat。同じ種類に同じ名前を 2 度登録すると落ちる。走り出す前に resolve_all で全部解決する。",
  "rules.md 10-1（2026-09-14）: モデル名（Ridge+GAN増強(batch16k)）は台帳の鍵（catalog.KEY）。増強は + で足し、既定から外した水準だけ括弧で付ける。実験名（trade_ownex_mlpgan16k_b）は鍵ではない ＝ 名前から中身を読まない・正本は config の中身。leak 対照は実行ディレクトリにだけ _leak が付く。",
]
detail_links = [{ label = "rules.md 10-1（名前の付け方）", doc = "docs/specs/experiments/feature-discovery/rules.md" }]

[page.section.detail_table]
head = ["何の名前か", "形", "例", "どこで決まるか"]
rows = [
  ["学習器", "登録名", "Ridge ／ LightGBM ／ MLP ／ PatchTST ／ Ridge+GAN増強(batch16k)", "ail/models/*.py の @register(\"model\", …)"],
  ["検知器", "登録名", "T3 QUANT（60日窓）／ D2 中期ゲート（60日・学習）／ 入口D1(20)×出口D3(200)（学習）", "ail/detectors/*.py"],
  ["選別・変換", "登録名", "全部使う（基準）／ F4-1 記号回帰（遺伝的プログラミング）", "ail/selectors/・ail/search/"],
  ["実験", "config の name ＝ ファイル名", "trade_ownseq_ridge_a（trade_ ＋ 表の呼び名 ＋ モデル略 ＋ 形式）", "config/experiment/<name>.toml"],
  ["実行", "<時刻>_<実験名>[_leak]", "2026-09-19T13-19-20_trade_ownseq_ridge_a_topk", "runs/（1 実行 1 ディレクトリ）"],
  ["トレーダー", "設定のファイル名", "T1〜T3 ／ test_a ／ sim_T1", "live-trading/config/traders/<名前>.toml"],
  ["画面のページ", "id", "own-ridge ／ ownex-lgbm ／ seq-quant", "dashboard/models.toml"],
]
