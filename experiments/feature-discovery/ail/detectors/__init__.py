"""検知器 — ⚠ **買い% 1 本だけを返すもの**（rules.md 14-1 の出力の契約）。

⚠ **検証側は中身を知らない。** 分割・パージ・シミュレータ・基準線・判定は、
⚠ **銘柄 s・日 t の買い%（0〜100）の系列だけ**を受け取る。だからスケールの合成でも
レジーム分岐でも、ここに 1 本足せば同じ物差しで測れる。

    fn(訓練, 検証, 列, ctx) -> (買い%（検証の行数）, 記録に残す辞書)

⚠ **fit は訓練分割の内側だけ**（rules.md 3 章 B）。⚠ **検証の行は transform にしか使わない。**
"""
