# ⚠ **経済指標の発表日（暦）**（2026-09-09）。⚠ **値ではなく「日付そのもの」が情報である。**
#
# ⚠ **発表の予定日は事前に公表される**ので、「明日は発表日」を特徴量にしても先読みにならない
# （[プラン](../../../../docs/plans/econ-calendar.md)）。発表された**値**を使うなら `ex_` 層のずらし規約に載せる。
#
# ⚠ **BLS（雇用統計・CPI）は採らない** — robots.txt 自体が 403【実測 2026-09-09】。
#    UA の偽装で回り込まない（SPC・Stooq と同じ扱い）。FRED の releases API は「要判断」に合流。
# ⚠ **予定された会合だけ。** 緊急会合と notation vote は事前に公表されないので暦に入れない。
name = "econ_calendar"
period = "d"

[[series]]
source = "fomc"
role = "本命"
hypothesis = "⚠ 政策決定の発表日は値動きの分布が変わる（発表までは方向を取らず、発表後に動く＝ボラが上がる）"
ids = ["decision"]
start = "2018-01-01"
end = "2027-12-31"
note = "⚠ 暦（発表日に行・値 1）。予定された会合の最終日＝声明の発表日（14:00 ET）。未来の予定も残す（検証は過去分だけ）"
