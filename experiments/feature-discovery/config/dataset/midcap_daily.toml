# 中位の銘柄（段 3 の対象）の日足。⚠ **取得元と上限は `daily` と同じ**（1 購読 8,000 本 = 最大 32 年）。
# ⚠ **`daily.toml` は書き換えていない。**
name = "midcap_daily"
source = "tastytrade"
period = "d"
days = 12000
universe = "midcap50"
adjust = "split"
