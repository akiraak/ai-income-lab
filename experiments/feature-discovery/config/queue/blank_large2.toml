# ⚠ **台帳の空白「大」2 件を埋めるキュー**（プラン `plans/archive/ledger-blanks-large-two.md` §5 の Phase 3）。
#
# ⚠ **これが通ると、カタログの実施可能な空白は 0 件になる**（残るは見送り 3 件 — F4-2・F5-2・F2-4）。
#
# ⚠ **本番 1 本につき `--leak` 対照が自動で 1 本並ぶ**（`leak = true`。rules.md 14-10 規約 6）。
# ⚠ **手で足すと必ず忘れる**ので、器の既定に寄せてある。
#
# ⚠ **事前登録**（回す前に決めた。結果を見て動かさない）: 本番 2 ＋ leak 2 ＝ 4 実行 ／
# ⚠ **n_trials 598 → 604（＋6 ＝ 2 手法 × θ 3 水準）** ／ 判定は対 B&H 上乗せの符号（13-7。B&H ＝ ＋1,652.02bp/fold）。
#
# ⚠ **順は重い順ではなく「空白の ID 順」**（F4-3 → F5-3）。⚠ **結果を見て並べ替えない。**
#
# ⚠ **打ち切りは 3 つとも書く**（14-9）。⚠ **時間の見積りは実測から**（プラン §7-1）:
# ⚠ **tsfresh 22.9 分/実行 ＋ stumpy 8.0 分/実行 ＝ 4 実行で約 62 分。**
experiments = ["trade_ownseq_tsf_a", "trade_ownseq_mp_a"]

leak = true              # ⚠ 配線の穴に気づく唯一の手。切らない
ledger = true            # ⚠ 本番の後に台帳を吐き直し、n_trials の推移を状態に残す
max_failures = 3         # ⚠ 連続で失敗したら止める（直してから同じコマンドで再開する）
time_budget_s = 7200     # ⚠ キュー全体の時間の上限（2 時間。⚠ **見積り 62 分の約 2 倍**）
