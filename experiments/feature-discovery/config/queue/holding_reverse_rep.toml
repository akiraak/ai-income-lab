# ⚠ **保有日数・逆売買の Phase 3 を 1 回で兼ねる代表構成の回し直し**（TODO「代表 1 構成 ＋ leak 対照を 1 回だけ回し直す」）。
#
# ⚠ **代表は結果を見る前に決めた（2026-09-17 16:55）**: `trade_ownseq_ridge_a`（台帳で 2026-09-15 以降の `trade_*` の最上位
# 「T3 QUANT（60日窓）」θ=50 ＋1,713.93bp・保留）。旧実行 `2026-09-15T10-38-12`（＋ leak `10-55-17`）。
# ⚠ **同じ表（`data/features/trade_ownseq_ridge_a`・09-15 に構築）・同じ config・同じ種**なので、既存列は旧実行と 1 ビットも
# 変わらないはず ＝ 「保有日数・逆売買の列を足しても既定の計算を変えていない」の実物での検算（rules.md 14-4 規約 2）。
#
# ⚠ **事前登録**: 同じ鍵の再実行なので **n_trials は 604 のまま**。台帳の行も増えない。
# ⚠ **台帳はここでは吐かない**（Phase 4 で 1 回吐く。判定が変わる見込みは無い）。
experiments = ["trade_ownseq_ridge_a"]

leak = true              # ⚠ 配線の穴に気づく唯一の手。切らない
ledger = false           # ⚠ 終わってから本体で 1 回吐く
max_failures = 3
time_budget_s = 7200     # ⚠ 1 本 約 17 分【実測】× 2 の 3.5 倍
