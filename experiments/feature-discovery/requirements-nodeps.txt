# ⚠ **`pip install --no-deps -r requirements-nodeps.txt` で入れる**（requirements.txt の後に）。
#
# ⚠ **なぜ依存なしか**: aeon 1.5.0 は `numba<0.64` を宣言し、numba 0.63 は `numpy<2.4` なので、
# ⚠ **宣言どおりに入れると numpy が 2.4.2 → 2.3.5 に下がる**【実測 2026-09-15。pip の依存解決】。
# ⚠ **これまでの全実行は numpy 2.4.2 で記録されている**ので下げない。
# ⚠ **代わりに numba 0.67.0 と組み合わせ、宣言どおりの環境（numba 0.63.1・numpy 2.3.5）と
# ⚠ MiniRocket・Hydra・QUANT の出力がビット単位で一致することを確かめた**（2026-09-15。
# docs/plans/tsc-minirocket-hydra-quant.md §4 の 3）。⚠ **`pip check` は aeon の食い違いを 1 件出すが想定どおり。**
# ⚠ **aeon は `ail/detectors/tsc.py` の関数の中でだけ import する**（既存の実行は numba を読み込まない）。
aeon==1.5.0          # BSD-3-Clause
Deprecated==1.3.1    # aeon の依存
wrapt==2.4.1         # Deprecated の依存
