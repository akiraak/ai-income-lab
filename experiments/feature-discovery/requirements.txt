# 特徴量の発見手法の検証。⚠ **取得もこの venv で通す**（2026-09-09）。
# ⚠ **以前は「取得はサンプル側の venv で」と書いてあったが、そちらに pandas が無いので実際には通らなかった。**
numpy==2.4.2
pandas==2.3.3
scipy==1.17.1
scikit-learn==1.7.2
pyarrow==25.0.1      # data/features/*.parquet（CSV だと 1 実験 300MB になる）
pytest==9.1.1        # tests/（不変条件・調整の検算・先読み）
requests==2.32.5     # ⚠ 取得（ttclient が使う）。サンプルと同じ版に固定する
websockets==15.0.1   # ⚠ 取得（DXLink）。同上
lightgbm==4.7.0      # 勾配ブースティング（plans/gpu-models.md §1。⚠ この規模なら CPU で足りる）
torch==2.14.0        # MLP・WGAN-GP（同 §1・§2）。既定 wheel は CUDA ランタイム同梱（+cu130）
# ⚠ 時系列分類器（MiniRocket・Hydra・QUANT。plans/tsc-minirocket-hydra-quant.md）の土台。
# ⚠ **aeon 本体は requirements-nodeps.txt に分けて `--no-deps` で入れる**（あちらの注記）
numba==0.67.0        # ⚠ numpy 2.4.2 を保てる版。aeon の宣言（numba<0.64）の外
llvmlite==0.49.0     # numba の依存
# ⚠ 台帳の空白 F5-4 ウェーブレット（plans/ledger-blanks-six.md）。⚠ **単体で入り、既存の版を 1 つも動かさない**
# （2026-09-15 に `pip install --dry-run` で確認）。⚠ **F3-4 の shap・F3-6 の knockpy・F1-6 の dcor は入れない**
# （順に LightGBM の pred_contrib ・ 自前の等相関構成 ・ 自前の距離相関で代替した。プラン §0-1）
PyWavelets==1.10.0
